//Imports

//Custom modules

//Layout
const Layout = ({ children, className }) => {
  //Router

  //Hooks

  //Objects

  //Functions
  return (
    <div className={`min-h-screen h-screen w-screen bg-white ${className}`}>
      {children}
    </div>
  );
};

export default Layout;
